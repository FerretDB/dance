---
runner: command
dir: python-example
args: ["sh", "-c", "pip3 install pymongo && python3 pymongo_test.py mongodb://localhost:27017/"]

results:
  common:
    stats:
      expected_pass: 1

  ferretdb:
    stats:

  mongodb:
    stats:
