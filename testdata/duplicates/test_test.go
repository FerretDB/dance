package test

import (
	"testing"
)

func TestDuplicates(t *testing.T) {
	t.Parallel()
	// TODO: We should add here some test cases which would just Pass/Fail or return output required for tests

	//TestFoo is the last part of the testname in the yaml file.
	t.Run("TestFoo", func(t *testing.T) {
	})
}
