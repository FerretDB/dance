net:
  port: 27017
  bindIpAll: true
  tls:
    mode: disabled

security:
  authorization: enabled
  keyFile: "/etc/key"

replication:
   oplogSizeMB: 512
   replSetName: rs0

setParameter:
  enableTestCommands: 1

systemLog:
  quiet: true
