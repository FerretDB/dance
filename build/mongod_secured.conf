net:
  port: 47018
  bindIpAll: true
  tls:
    mode: disabled

security:
  authorization: enabled # that also enables authentication
  clusterAuthMode: x509

# replication:
#   oplogSizeMB: 512
#   replSetName: rs0
